export function FooterBlur() {
  return (
    <div
      className="absolute bottom-0 left-0 right-0 h-8 pointer-events-none z-30"
      // style={{ background: "linear-gradient(to top, #fdfdfd 40%, transparent)" }}
    />
  );
}
